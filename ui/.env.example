# Leave empty in local dev — requests go to http://127.0.0.1:8000 automatically
VITE_API_URL=

# Supabase — not required for localhost demo (evaluations use localStorage)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
