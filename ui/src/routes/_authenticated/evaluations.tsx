import{createFileRoute,Link}from"@tanstack/react-router";import{useEffect,useState}from"react";import{Search,Trash2,Plus}from"lucide-react";import{AppShell}from"@/components/app-shell";import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{supabase}from"@/integrations/supabase/client";import type{Tables}from"@/integrations/supabase/types";export const Route=createFileRoute("/_authenticated/evaluations")({component:Evaluations});function Evaluations(){const[rows,setRows]=useState<Tables<"evaluations">[]>([]),[q,setQ]=useState("");const load=()=>supabase.from("evaluations").select("*").order("created_at",{ascending:false}).then(({data})=>setRows(data??[]));useEffect(()=>{load()},[]);const del=async(id:string)=>{await supabase.from("evaluations").delete().eq("id",id);load()};return <AppShell><div className="flex items-center justify-between"><div><h1 className="text-3xl">All Evaluations</h1><p className="mt-2 text-xs text-muted-foreground">Your startup ideas, drafts, and reports.</p></div><Button variant="hero" asChild><Link to="/new-evaluation"><Plus/>New</Link></Button></div><div className="relative mt-8 max-w-sm"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground"/><Input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search reports…" className="pl-9"/></div><div className="mt-5 overflow-x-auto border border-border bg-card"><table className="w-full min-w-[700px] text-left text-xs"><thead className="border-b border-border text-[10px] uppercase tracking-wider text-muted-foreground"><tr><th className="p-4">Idea</th><th>Status</th><th>Score</th><th>Date</th><th>Actions</th></tr></thead><tbody>{rows.filter(r=>r.title.toLowerCase().includes(q.toLowerCase())).map(r=><tr key={r.id} className="border-b border-border last:border-0"><td className="p-4 font-medium">{r.title}</td><td className="capitalize text-success">{r.status}</td><td>{r.overall_score??"—"}</td><td className="text-muted-foreground">{new Date(r.created_at).toLocaleDateString()}</td><td><div className="flex gap-2"><Button variant="ghost" size="sm" asChild><Link to={r.status==="completed"?"/results/$id":"/evaluation/$id"} params={{id:r.id}}>View</Link></Button><Button variant="ghost" size="icon" onClick={()=>del(r.id)}><Trash2/></Button></div></td></tr>)}</tbody></table>{rows.length===0&&<p className="p-10 text-center text-xs text-muted-foreground">No evaluations yet.</p>}</div></AppShell>}