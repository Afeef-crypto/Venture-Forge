import{createFileRoute}from"@tanstack/react-router";import{AppShell}from"@/components/app-shell";import{Button}from"@/components/ui/button";import{Copy,Download}from"lucide-react";export const Route=createFileRoute("/_authenticated/tasks")({component:Tasks});function Tasks(){const cols=["Frontend","Backend","Database","Design"];const exportTasks=()=>{const text=cols.map(c=>`## ${c}\n- Create core experience\n- Implement data flow\n- Add quality checks`).join("\n\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type:"text/markdown"}));a.download="cursor-ready-tasks.md";a.click();URL.revokeObjectURL(a.href)};return <AppShell><div className="flex justify-between"><div><h1 className="text-3xl">Cursor-Ready Tasks</h1><p className="mt-2 text-xs text-muted-foreground">AI-generated tasks optimized for development.</p></div><Button variant="outline" onClick={exportTasks}><Download/>Export Tasks</Button></div><div className="mt-8 grid gap-4 lg:grid-cols-4">{cols.map((c,i)=><section key={c}><h2 className="font-sans text-xs font-semibold">{c} ({i+2})</h2>{["Create core experience","Implement data flow","Add quality checks"].slice(0,i%2+2).map((t,j)=><article key={t} className="mt-3 border border-border bg-card p-4"><div className="flex justify-between gap-2"><b className="text-xs">{t}</b><Button variant="ghost" size="icon" onClick={()=>navigator.clipboard.writeText(t)} aria-label={`Copy ${t}`}><Copy/></Button></div><p className="mt-2 text-[10px] leading-4 text-muted-foreground">Production-ready implementation with clear acceptance criteria.</p><span className="mt-3 inline-block text-[9px] text-primary">{j===0?"High":"Medium"}</span></article>)}</section>)}</div></AppShell>}