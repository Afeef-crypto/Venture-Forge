import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { ArrowLeft, ArrowRight, Save, Trash2 } from "lucide-react";
import { z } from "zod";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
import { supabase } from "@/integrations/supabase/client";

export const Route=createFileRoute("/_authenticated/new-evaluation")({validateSearch:(search)=>z.object({template:z.string().optional(),idea:z.string().optional(),industry:z.string().optional(),stage:z.string().optional()}).parse(search),component:NewEvaluation});
const demos=[{title:"AI Study Companion",text:"AI copilot that helps students find and summarize notes from previous years."},{title:"FinTrack",text:"Personal finance app for freelancers."},{title:"LocalEats",text:"Hyperlocal food delivery marketplace."},{title:"CodeReview AI",text:"AI-powered code review assistant."}];
function NewEvaluation(){const {user}=Route.useRouteContext();const search=Route.useSearch();const nav=useNavigate();const [idea,setIdea]=useState(search.idea??"");const [industry,setIndustry]=useState(search.industry??"Technology");const [stage,setStage]=useState(search.stage??"Idea");const [depth,setDepth]=useState("Standard");const [research,setResearch]=useState(true);const [busy,setBusy]=useState(false);
const save=async(run:boolean)=>{if(idea.trim().length<10)return;setBusy(true);const title=idea.split(/[.!?]/)[0].slice(0,60)||"Untitled startup";const {data,error}=await supabase.from("evaluations").insert({user_id:user.id,title,idea,industry,stage,evaluation_depth:depth,use_web_research:research,status:run?"running":"draft",progress:run?8:0}).select("id").single();setBusy(false);if(!error&&data)nav({to:run?"/evaluation/$id":"/evaluations",params:run?{id:data.id}:undefined as never});};
 return <AppShell><Button variant="ghost" size="sm" onClick={()=>history.back()} className="mb-7"><ArrowLeft/>Back</Button><h1 className="text-4xl">Describe your startup</h1><p className="mt-2 text-xs text-muted-foreground">The more details you provide, the better our agents can evaluate.</p><textarea value={idea} onChange={e=>setIdea(e.target.value)} placeholder="Describe your startup idea…" className="mt-8 h-48 w-full resize-none border border-input bg-card p-5 text-sm outline-none focus:ring-1 focus:ring-ring"/><div className="mt-5 grid gap-4 sm:grid-cols-4"><label className="text-[10px] text-muted-foreground">Industry<select value={industry} onChange={e=>setIndustry(e.target.value)} className="mt-2 h-10 w-full border border-input bg-card px-3 text-xs text-foreground"><option>Technology</option><option>Fintech</option><option>Healthcare</option><option>Marketplace</option></select></label><label className="text-[10px] text-muted-foreground">Stage<select value={stage} onChange={e=>setStage(e.target.value)} className="mt-2 h-10 w-full border border-input bg-card px-3 text-xs text-foreground"><option>Idea</option><option>MVP</option><option>Early Revenue</option></select></label><label className="text-[10px] text-muted-foreground">Evaluation Depth<select value={depth} onChange={e=>setDepth(e.target.value)} className="mt-2 h-10 w-full border border-input bg-card px-3 text-xs text-foreground"><option>Standard</option><option>Deep Dive</option></select></label><div className="flex items-end gap-3 pb-2 text-xs"><Switch checked={research} onCheckedChange={setResearch}/>Use Live Web Research</div></div><p className="mb-4 mt-10 text-xs font-semibold">Or try a demo preset</p><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{demos.map(d=><Button key={d.title} variant="outline" onClick={()=>setIdea(d.text)} className="h-auto items-start justify-start whitespace-normal p-5 text-left"><span><b className="text-xs">{d.title}</b><span className="mt-2 block text-[10px] leading-4 text-muted-foreground">{d.text}</span></span></Button>)}</div><div className="mt-10 flex flex-wrap justify-end gap-3"><Button variant="ghost" onClick={()=>setIdea("")}><Trash2/>Clear Form</Button><Button variant="outline" onClick={()=>save(false)} disabled={busy||idea.length<10}><Save/>Save Draft</Button><Button variant="hero" onClick={()=>save(true)} disabled={busy||idea.length<10}>Run Evaluation <ArrowRight/></Button></div></AppShell>}