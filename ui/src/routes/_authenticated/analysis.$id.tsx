import { createFileRoute, Link } from "@tanstack/react-router";
import { z } from "zod";import{useEffect,useState}from"react";import{ArrowLeft,CheckCircle2,TriangleAlert}from"lucide-react";import{AppShell}from"@/components/app-shell";import{supabase}from"@/integrations/supabase/client";
const search=z.object({agent:z.string().catch("YC Partner")});type Agent={name:string;score:number;analysis:string;recommendations:string[];risks:string[]};
export const Route=createFileRoute("/_authenticated/analysis/$id")({validateSearch:search,component:Analysis});
function Analysis(){const{id}=Route.useParams();const{agent}=Route.useSearch();const[list,setList]=useState<Agent[]>([]);useEffect(()=>{supabase.from("evaluations").select("agent_results").eq("id",id).single().then(({data})=>setList((data?.agent_results??[])as unknown as Agent[]))},[id]);const active=list.find(a=>a.name===agent)??list[0];return <AppShell><Link to="/results/$id" params={{id}} className="flex items-center gap-2 text-xs text-muted-foreground"><ArrowLeft className="h-4 w-4"/>Overview</Link><div className="mt-7 flex gap-6 overflow-x-auto border-b border-border">{list.map(a=><Link key={a.name} to="/analysis/$id" params={{id}} search={{agent:a.name}} className={`shrink-0 border-b-2 pb-3 text-xs ${a.name===active?.name?"border-primary text-primary":"border-transparent text-muted-foreground"}`}>{a.name}</Link>)}</div>{active&&<><header className="mt-8 flex items-start justify-between"><div><h1 className="text-3xl">{active.name}</h1><p className="mt-1 text-xs text-muted-foreground">Detailed specialist analysis</p></div><div className="text-right text-xs text-muted-foreground">Score <b className="block text-3xl text-success">{active.score}<span className="text-xs"> /100</span></b></div></header><section className="mt-7 border border-border bg-card p-7"><h2 className="font-sans text-sm font-semibold">Detailed Analysis</h2><p className="mt-4 text-sm leading-7 text-muted-foreground">{active.analysis}</p><div className="mt-8 grid gap-8 md:grid-cols-2"><div><h3 className="font-sans text-xs font-semibold">Recommendations</h3><ul className="mt-4 space-y-3">{active.recommendations.map(x=><li key={x} className="flex gap-2 text-xs leading-5"><CheckCircle2 className="h-4 w-4 shrink-0 text-success"/>{x}</li>)}</ul></div><div><h3 className="font-sans text-xs font-semibold">Key Risks</h3><ul className="mt-4 space-y-3">{active.risks.map(x=><li key={x} className="flex gap-2 text-xs leading-5"><TriangleAlert className="h-4 w-4 shrink-0 text-warning"/>{x}</li>)}</ul></div></div></section><div className="mt-5 border-l-2 border-primary bg-accent p-5 text-xs"><b>Recommendation</b><p className="mt-2 text-muted-foreground">Proceed with a focused MVP and validate the critical assumption before expanding scope.</p></div></>}</AppShell>}